anthropic>=0.28
aio-pika>=9.4
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0
redis[asyncio]>=5.0
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.2
pyyaml>=6.0
docker>=7.1
# Required for AES-GCM decryption of per-session AI keys.
# Comment out (and the code will fall back to base64 — INSECURE) if PyPI is unreachable.
cryptography>=42.0
