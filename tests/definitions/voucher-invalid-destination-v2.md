---
id: voucher-invalid-destination-v2
name: Voucher with invalid destination address — execution reverts (v2.x)
version: 1
min_node_major_version: 2
tags: [outputs, voucher, error-handling, v2, phase5]
csv_ids: ["5.5"]
release_introduced: v2.0.0
component: advancer
priority: medium
timeout_seconds: 120
requires:
  - anvil
  - cartesi-node-v2
assertions:
  - type: chain_tx
    payload: "0x7b22616374696f6e223a22766f75636865725f6261645f64657374222c2264657374223a22307830303030303030303030303030303030303030303030303030303030303030303030303030303030227d"
    comment: "request voucher to zero address (invalid destination)"
  - type: voucher_v2
    mode: generate
    expect_count: 1
    poll_timeout: 60
---

## Description
CSV test 5.5 — Request the application to emit a voucher with an invalid (zero)
destination address and verify the voucher is generated but execution reverts
when attempted on-chain.

## Steps
1. Submit an input requesting a voucher to address(0).
2. Assert the voucher is generated by the VM.
3. (Note: On-chain execution would revert — this test confirms generation.)

## Expected Behaviour
- VM emits the voucher as requested (VM does not validate destination).
- Voucher appears in cartesi_listOutputs.
- Execution on-chain would revert (not tested here — separate execute test).
